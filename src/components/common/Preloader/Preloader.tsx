import Spinner from '../../../assets/images/Spinner.gif';
import React from 'react';

const Preloader: React.FC = () => {
  return <img src={Spinner} width={100} alt="Spinner" />
}

export default Preloader;
